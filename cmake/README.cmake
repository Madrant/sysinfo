Use 'build.bash' script to build the package

Packages required to build the package:
    cmake (apt-get install cmake)

Commands to build package manually:
    cd build
    cmake ../
    make package
